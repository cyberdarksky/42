#include <stdio.h>
int main()
{
    int x = 153;
    int y = 153 % 2;
    printf("Moddan sonra: %d\n", y);
    x = x / 10;
    printf("Bolmeden sonra: %d\n\n", x);
    return 0;
} // şimdi artık 5'i birler basamağına çekmiş oldum // müq zeka bu işlem her yerde yok xd
// sonra 15'in modunu alıcam
// 15'i 10'a bölünce 1 vericek
// onu da birler basamağı olrak alıcam
// burayı berbat anlattım dimi?
// sizin ses yeni geldi xd tekrar söy