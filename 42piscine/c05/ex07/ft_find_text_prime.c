#include <stdio.h>
int ft_find_next_prime(int nb)
{
	int x;
	x = 0;
	if(nb <= 2)
		return (2);
	f(nb % )
	while()
	{
		if((sayi % n) == 0)

	}
}

int main(void)
{
	printf("%d\n", ft_find_next_prime(12));
	return 0;
}
